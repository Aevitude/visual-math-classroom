---
name: visual-math-classroom
description: >
  做「可视化学堂」级互动数学课件：单文件 HTML，左舞台右老师，
  探索/跟我学/闯关/出题四模式，数字全部由 solveFor 算出，动画本身就是证明。
  用于任何小学到初中的讲题、课件、可视化数学、鸡兔同笼、蜡烛燃烧、
  行程、注水、植树、和差、方程。用户说「做一道题」「做个课件」时启用。
metadata:
  short-description: 可视化学堂：一题一舞台、四模式、数字只来自 SOLVE
---

# 可视化学堂 · Visual Math Classroom

做一节**能投到教室里、学生能自己玩懂**的互动讲题。质量地板是
`examples/chickens-rabbits.html`。不是教案长文，不是紫色教育后台，不是 emoji 贴图。

**先读本文件。** 再按需打开 `references/`，不要一次读完。

| 你要做的事 | 打开 |
|---|---|
| 任何课件 | 本文件 + `references/anti-slop.md` |
| 定数字 / 题面 / 旁白 | `references/problem-contract.md` |
| 定四模式各自发生什么 | `references/pedagogy-loop.md` |
| 定舞台长什么样 | `references/stage-metaphor.md` |
| 写阿临的台词 | `references/teacher-voice.md` |
| 定纸墨颜色和字体 | `references/visual-system.md` |
| 从金样克隆底盘 | `references/chassis-contract.md` |
| 鸡兔 / 头脚计数 | `references/genres/chickens-rabbits.md` |
| 蜡烛 / 注水 / 行程 / 工程 | `references/genres/uniform-rate.md` |
| 为什么这样教 | `references/philosophy.md` |

输出默认是**一个完整的单文件 HTML**（CSS+SVG+JS 全在里面）。不要 npm，不要 React，不要多文件工程。

---

## 0. 决策树（先走这里）

用户一句话进来，先归族，再动手。

```
题目在说什么？
│
├─ 两种东西、两种「脚/轮/腿」，已知总数和总量
│    → 计数族（鸡兔、蟹蜻蜓、三轮车自行车）
│    → genres/chickens-rabbits.md
│    → 主控件 = 两种数量的 +/-
│
├─ 东西按固定速度变少/变多，问「何时一样 / 还剩多少 / 还要多久」
│    → 匀速族（蜡烛燃烧、注水排水、行程相遇追及、工程）
│    → genres/uniform-rate.md
│    → 主控件 = 时间轴，不是加减只数
│
├─ 路、点、间隔（植树、路灯、锯木头）
│    → 离散位置族：舞台是路 + 点。主控件 = 段数/点数
│
└─ 和、差、倍、份
     → 条形模型族：舞台是条。主控件 = 各部分长度
```

**用户只说「做个蜡烛燃烧」**，不要再问。默认题：

> 甲蜡烛高 24 cm，6 小时燃尽；乙蜡烛高 16 cm，8 小时燃尽。同时点燃，多久后一样高？

算出：甲速 4 cm/h，乙速 2 cm/h，t = 4 h，那时各剩 8 cm。  
这是匀速族。舞台是桌子上两支会变矮的蜡烛 + 时间轴。禁止画鸡兔围栏。

---

## 1. 硬合同（违反任何一条算出品失败）

### 数

1. 题面、旁白、按钮、答案卡、坐标轴标签里，**禁止手写数字字面量**。全部来自 `PROBLEM` / `SOLVE`。
2. 文件里只允许一处放「题的原始数据」：`const PROBLEM = { ... }`。
3. 所有派生量必须经过 `solveFor(PROBLEM)`。旁白用模板字符串插 `S.xxx` / `P.xxx`。
4. 闯关题必须**先抽整数答案，再反推已知量**。禁止随机已知量再碰运气。

### 教

5. 四个模式齐：`explore` 自由探索 / `teach` 跟我学 / `challenge` 闯关 / `create` 出题。
6. 跟我学至少 **两种方法**，最后汇到同一组答案。
7. 每一步：**先改舞台，再说话**。按钮文案预告下一句洞察，禁止永远写「下一步」。
8. 切换模式或方法必须 `runToken++`，旧的 `await` 立刻作废。

### 看

9. 一种题型一个舞台隐喻。匀速族禁止复用鸡兔围栏和加减只数。
10. 生物/蜡烛/水槽用手绘 SVG（几何即可），禁止 emoji，禁止照片。
11. 调色：纸/墨/一种强调色。禁止紫渐变、禁止仪表盘蓝、禁止彩虹标签。
12. 桌面 fit-to-viewport（给投影用，不滚动）；手机才允许滚。

### 演

13. 动画必须改变计数、结构或证明状态。纯装饰弹跳算出品失败。
14. 学生应能从画面读出当前那句数学（蜡烛变矮 = 剩余；两焰等高 = 相等）。

---

## 2. 产品形状（底盘锁死，不许「优化」掉）

```
[ 开场仪式：一点才进教室 ]

[ 章级标签 · 中文题名 · 一句英文副题 ]

[ 舞台：活的观察区 ]     [ 题面 ]
  主计量会跳、会变绿       [ 方法页签（仅教学）]
  空态有一句话             [ 控件：本题的主变量 ]
                          [ 阿临 + 主按钮 ]
                          [ 答案卡（算完才出现）]
                          [ 四模式切换 ]
```

金样在 `examples/chickens-rabbits.html`。克隆时：

- **整份留下**：开场、顶栏、舞台+面板栅格、四模式、阿临、打字机、`runToken`、`interp`、`bootProblem`、声音开关、庆祝、投屏 CSS、触控 CSS。
- **整份换掉**：`PROBLEM` 形状、`solveFor`、SVG、舞台内部、控件、方法剧本、全部旁白。
- 细节清单见 `references/chassis-contract.md`。

---

## 3. 老师是谁

老师名叫**阿临**。像一个会停顿的人，不像课本，也不像幼教。

- 打字机输出，逗号句号处多停一会。
- 按钮是悬念：「那地上还有多少脚？」「先让时间走一小时看看」。
- 强调色：甲用 `.em`，乙用 `.em-r`，结论用 `.em-g`。
- 完整语气见 `references/teacher-voice.md`。

---

## 4. 开工顺序（每次都按这个）

1. 归族（§0）。打开对应 `genres/*.md`。
2. 写 `PROBLEM` + `solveFor`，先在脑子里跑通默认题，确认整数/干净分数。
3. 定舞台隐喻和**主控件**（计数=只数；匀速=时间轴）。
4. 从金样复制底盘 CSS/JS 基础设施，删掉旧隐喻。
5. 写四模式。教学至少两法，每步先动画后旁白。
6. 用文末清单自检。不通过就改，不要先交。

---

## 5. 完工清单（交稿前必须全部打勾）

- [ ] 单文件 HTML，打开即用，无外部 JS 框架
- [ ] 有开场仪式，点一下才进
- [ ] 四模式都在，并能来回切（旧教学流程被 `runToken` 取消）
- [ ] 题面数字 == `PROBLEM`；旁白数字全部来自 `SOLVE`（全文搜旁白里的阿拉伯数字，对不上就改）
- [ ] 跟我学 ≥ 2 种方法，答案一致
- [ ] 每一步舞台有可见变化，不是只揭晓文本
- [ ] 主控件属于本族（蜡烛=时间轴，不是鸡+兔-）
- [ ] 无 emoji 当图标；无紫渐变；无「智能教育平台」皮肤
- [ ] 舞台是手绘 SVG，活的（闲时微动、状态变化有动画）
- [ ] 桌面一屏能看完；手机可用
- [ ] 闯关题保证有干净解
- [ ] 出题能预览并（可选）复制分享链接
- [ ] 答案卡只在解出后出现

做完把这份清单的勾选结果写在回复里。
