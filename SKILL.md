---
name: visual-math-classroom
description: >
  可视化学堂互动课件。只读本文件和 templates/shell.html。
  禁止打开 examples/。用于课件、讲题、蜡烛燃烧、鸡兔同笼、行程、注水。
---

# 可视化学堂

⛔ **不要打开 `examples/` 里任何文件。** 太长，读了会截断，然后卡住。

✅ 只读：`SKILL.md` + `templates/shell.html`  
✅ 做法：复制 `shell.html`，改心脏，交一个单文件 HTML。

---

## 用户说「蜡烛燃烧」时

不要问。默认题：

> 甲 24 cm、6 小时燃尽；乙 16 cm、8 小时燃尽。同时点燃，多久一样高？

`solveFor` 会得到：rateA=4，rateB=2，tEqual=4，remain=8。  
舞台 = 两支蜡烛 + 时间轴。主控件 = 拖时间。禁止鸡兔、禁止加减只数。

`templates/shell.html` 已经是这道题的可运行骨架。复制它，不要从零发明布局。

---

## 硬合同（破一条就重做）

1. 旁白 / 按钮 / 答案卡 **禁止手写数字**。全部 `${S.tEqual}` / `${P.actors[0].height}`。
2. 数据只放在 `PROBLEM`，派生只来自 `solveFor`。
3. 四模式齐：探索 / 跟我学 / 闯关 / 出题。
4. 跟我学 ≥ 2 法，答案都是同一组 `SOLVE`。
5. 每步先改舞台再说话。按钮是悬念，禁止永远写「下一步」。
6. `setMode` / `setMethod` 里 `runToken++`，旧 `await` 立刻停。
7. 蜡烛用手绘 SVG，火苗会晃。禁止 emoji，禁止紫渐变，禁止照片。
8. 桌面一屏能投。闯关先抽整数 t 再反推高和小时。

---

## 你要改 shell 的哪些地方

留下：开场、左右布局、tokens、打字机、`runToken`、四模式、阿临、时间轴、`PROBLEM`/`SOLVE`。

可以加强：火苗 SVG、蜡油、跟我学三法的分步动画和台词、闯关生成、出题预览。

不要改成：React、多文件、教育后台、鸡笼、加减蜡烛只数。

老师叫 **阿临**。短句，先指画面。

---

## 跟我学三法（都要做到 tEqual、remain）

1. **单位时间**：报速率 → 一小时一小时拨时间，蜡变矮。
2. **方程**：剩余 = 高 − 速率×t，两式相等，交点就是答案。
3. **速度差**（建议有）：一开始高差 `heightGap0`，每小时多消 `rateDiff`，t = 高差/速差。高差虚线缩到 0。

---

## 交稿清单

- [ ] 一个 HTML，没去读 examples/
- [ ] 开场 + 四模式 + runToken
- [ ] 旁白无手写数字
- [ ] 教学 ≥ 2 法，答案 4 小时 / 剩 8 cm
- [ ] 时间轴，蜡烛会矮，火苗会动
- [ ] 无 emoji / 无紫
- [ ] 闯关能解，答案卡解出才出现
