# 从这里开始（只读两个文件）

1. `SKILL.md`（规则，很短）
2. `templates/shell.html`（直接可运行的蜡烛课件骨架）

**禁止打开 `examples/`。** 那个金样 86KB，会读一半就截断，不要下。

做法：把 `templates/shell.html` 整份复制出来，按 `SKILL.md` 检查一遍，补好看的火苗和跟我学台词，输出**一个** HTML。不要从空白重写，不要再去找鸡兔文件。
